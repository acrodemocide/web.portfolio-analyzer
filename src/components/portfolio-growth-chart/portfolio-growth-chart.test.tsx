import { PortfolioGrowthChart } from './portfolio-growth-chart';

describe('portfolio-growth-chart', () => {
    it('passes the test', () => {
        expect(true).toBe(true);
        expect(PortfolioGrowthChart).toBeDefined();
    });
});
